// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('app').config(function($provide) {
    return $provide.decorator('ratingDirective', function($delegate) {
      $delegate[0].templateUrl = '/partials/bootstrap/rating/rating';
      return $delegate;
    });
  }).controller('CourseDetailsController', function($scope, CachedCourse, $routeParams, IdentityService, NotifierService, $location) {
    $scope.identity = IdentityService;
    $scope["delete"] = function() {
      return CachedCourse.remove($routeParams.id).then(function() {
        NotifierService.notify("Course removed successfully");
        return $location.path("/");
      }, function(error) {
        return NotifierService.error(error);
      });
    };
    $scope.myRate = 2;
    $scope.max = 5;
    $scope.rate = function(rate) {
      console.log(rate);
      return $scope.myRate = rate;
    };
    $scope.rankHover = function(rate) {
      console.log(rate);
      return $scope.rate = rate;
    };
    $scope.rankReset = function() {
      return $scope.rate = $scope.myRate;
    };
    $scope.rankReset();
    return CachedCourse.query().$promise.then(function(collection) {
      return collection.findById($routeParams.id, function(course) {
        return $scope.course = course;
      });
    });
  });

}).call(this);

//# sourceMappingURL=CourseDetailsController.map

// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('app').controller('CourseDetailsController', function($scope, CachedCourse, $routeParams, IdentityService, NotifierService, $location) {
    $scope.identity = IdentityService;
    $scope["delete"] = function() {
      return CachedCourse.remove($routeParams.id).then(function() {
        NotifierService.notify("Course removed successfully");
        return $location.path("/");
      }, function(reason) {
        return NotifierService.error(reason);
      });
    };
    return CachedCourse.query().$promise.then(function(collection) {
      return collection.forEach(function(course) {
        if (course._id === $routeParams.id) {
          $scope.course = course;
          return false;
        }
      });
    });
  });

}).call(this);

//# sourceMappingURL=CourseDetailsController.map

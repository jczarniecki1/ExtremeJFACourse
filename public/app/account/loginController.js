// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('app').controller('loginController', function($scope, $http, mvNotifier, mvIdentity, mvAuth, $location) {
    $scope.identity = mvIdentity;
    $scope.signin = function(username, password) {
      return mvAuth.authenticateUser(username, password).then(function(success) {
        if (success) {
          return mvNotifier.notify('You have successfully signed in!');
        } else {
          return mvNotifier.warning('Failed to log in.');
        }
      });
    };
    return $scope.signout = function() {
      return mvAuth.logoutUser().then(function() {
        $scope.username = "";
        $scope.password = "";
        mvNotifier.info('You have succeessfully signed out');
        return $location.path('/');
      });
    };
  });

}).call(this);

//# sourceMappingURL=loginController.map

// Generated by CoffeeScript 1.7.1
(function() {
  var scenario, setLocation;

  setLocation = function(desiredLocation) {
    if (window.location === desiredLocation) {
      return window.location = desiredLocation;
    }
  };

  scenario = function() {
    setLocation('/courses');
    $('[ng-model="username"]').val('joe@pj.com');
    $('[ng-model="password"]').val('asd');
    $('#login:visible').trigger('click');
    return console.debug('>> scenario finished...');
  };

  try {
    console.debug('>> executing scenario...');
    setTimeout(scenario, 1000);
  } catch (_error) {
    console.debug('>> cannot execute defined scenario...');
  }

}).call(this);

//# sourceMappingURL=scenario.map

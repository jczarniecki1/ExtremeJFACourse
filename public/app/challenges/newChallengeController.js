// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('app').controller('newChallengeController', function($scope, mvChallengeEditor, $location, $routeParams, mvNotifier) {
    var courseId;
    courseId = $routeParams.courseId;
    return $scope.create = function() {
      var newChallengeData;
      newChallengeData = {
        description: $scope.description,
        body: $scope.body,
        score: $scope.score,
        courseId: courseId
      };
      return mvChallengeEditor.createChallenge(newChallengeData).then(function(challenge) {
        mvNotifier.notify('New challenge created successfully');
        return $location.path("/courses/" + courseId + "/challenge/" + challenge._id);
      }, function(reason) {
        return mvNotifier.error(reason);
      });
    };
  });

}).call(this);

//# sourceMappingURL=newChallengeController.map

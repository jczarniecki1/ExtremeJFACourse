// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('app').factory('ControlsProvider', function() {
    var ControlsProvider;
    ControlsProvider = (function() {
      function ControlsProvider() {
        this.controls = [
          {
            id: 'JFA-RegExp',
            name: 'JFA: RegExp Exercise',
            init: function($container, config, onSuccess) {
              var template;
              template = $("#regex-template").html();
              return $container.regexExercise(template, config, onSuccess);
            }
          }, {
            id: 'JFA-StateMachine',
            name: 'JFA: State Machine Exercise',
            init: function($container, config, onSuccess, onFail, afterQuiz) {
              var template;
              template = $("#automaton-template").html();
              return $container.automataExercise(template, config, onSuccess, onFail, afterQuiz);
            }
          }
        ];
      }

      ControlsProvider.prototype.getControls = function() {
        return this.controls.map(function(x) {
          var id, name;
          id = x.id, name = x.name;
          return {
            id: id,
            name: name
          };
        });
      };

      ControlsProvider.prototype.findById = function(id) {
        var foundControls;
        foundControls = this.controls.filter(function(x) {
          return x.id === id;
        });
        if (foundControls.length > 0) {
          return foundControls[0];
        } else {
          return null;
        }
      };

      return ControlsProvider;

    })();
    return new ControlsProvider();
  });

}).call(this);

//# sourceMappingURL=ControlsProvider.map
